import 'package:flutter/material.dart';

class NotFountScreen extends StatelessWidget {
  const NotFountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Not fount page"),
      ),
    );
  }
}
